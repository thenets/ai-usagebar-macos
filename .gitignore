/target
**/*.rs.bk

# Sandbox marker, not part of the project.
.ai-jail

# Claude Code local scratch state.
.claude/

# Local secrets — never commit
.env
.env.local
*.credentials.json

# Snapshot review files from `cargo insta`
*.snap.new

# macOS build artifacts (regenerate with macos/build.sh or macos/bundle.sh)
macos/ai-usagebar-menubar
macos/*.app/
.DS_Store
